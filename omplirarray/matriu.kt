fun main(){
    val tamanyarray = scanner.nextInt
    var array = arrayOf()

    for (i in 0 until array.size) {
        val numero = scanner.nextInt
        tamanyarray[i] = numero
    }

    for (fila in matriz) {
        for (valor in fila) {
            println(valor)
        }
    }

}